#!/bin/bash
#
option="o"
#
cmda="tail -f $HOME/direwolf.output"
cmdb="ls -al $HOME/direwolf.conf"
cmdc="direconfig"
cmdd="direswitch"
cmde="sudo service direwolf stop"
cmdf="sudo service direwolf start"
cmdg="sudo service direwolf restart"
cmdh="sudo service direwolf status"
cmdi="sudo systemctl enable direwolf.service"
cmdj="sudo systemctl disable direwolf.service"

while [ $option != "q" ] ; do
 echo ""
 echo "diremenu choices"
 echo ""
 echo "a) $cmda"
 echo "b) $cmdb"
 echo "c) $cmdc"
 echo "d) $cmdd"
 echo "e) $cmde"
 echo "f) $cmdf"
 echo "g) $cmdg"
 echo "h) $cmdh"
 echo "i) $cmdi"
 echo "j) $cmdj"
 echo "q) quit this menu"
 echo ""
 echo -e "Enter option :\c"
 read ans
 if [ -z $ans ] ; then
  ans="$option"
 fi
 option="$ans"
 case $option in
  a) $cmda ;;
  b) $cmdb ;;
  c) $cmdc ;;
  d) $cmdd ;;
  e) $cmde ;;
  f) $cmdf ;;
  g) $cmdg ;;
  h) $cmdh ;;
  i) $cmdi ;;
  j) $cmdj ;;
  q) echo "good bye"
 esac
done

